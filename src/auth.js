// auth.js — no auth, everything is local and free
// This module intentionally left empty as authentication is not required
// for the free local-first architecture of Prompt Autocomplete.
const Auth = (() => {
  return {};
})();
